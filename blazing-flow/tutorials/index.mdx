---
title: Tutorials Overview
label: Tutorials
description: Learn Blazing Flow through step-by-step tutorials
position: 0
---

Learn Blazing Flow through practical, step-by-step tutorials.

## Getting Started

### [Basic Task Execution](/docs/tutorials/basic-task-execution)

Create your first workflow with steps and execute it.

**You'll learn:**
- Define steps with `@app.step`
- Create workflows with `@app.workflow`
- Publish and execute workflows
- Handle results

**Time:** 5 minutes

---

### [Parallel Execution](/docs/tutorials/parallel-execution)

Execute multiple workflow instances concurrently for maximum throughput.

**You'll learn:**
- Run multiple workflow instances in parallel
- Control concurrency with Semaphore
- Optimize for different workload sizes
- Monitor parallel execution

**Time:** 10 minutes

---

## Advanced Topics

### [Error Handling](/docs/tutorials/error-handling)

Handle failures, edge cases, and ensure workflow resilience.

**You'll learn:**
- Validate parameters in steps
- Handle exceptions in workflows
- Implement retry logic
- Manage failures gracefully

**Time:** 15 minutes

---

### [Concurrency Patterns](/docs/tutorials/concurrency-patterns)

Master concurrency control and rate limiting patterns.

**You'll learn:**
- Use Semaphore for concurrency control
- Implement rate limiting
- Coordinate across workflow instances
- Optimize resource usage

**Time:** 20 minutes

---

## Quick Reference

### Basic Workflow Pattern

```python
import asyncio
from blazing import Blazing

async def main():
    app = Blazing()

    @app.step
    async def process(x: int, services=None):
        """Process a single value."""
        return x * 2

    @app.workflow
    async def my_workflow(value: int, services=None):
        """Complete workflow orchestration."""
        result = await process(value, services=services)
        return result

    await app.publish()

    # Execute workflow
    result = await app.my_workflow(5).wait_result()
    print(f"Result: {result}")  # Result: 10

if __name__ == "__main__":
    asyncio.run(main())
```

### Parallel Execution Pattern

```python
import asyncio
from blazing import Blazing

async def main():
    app = Blazing()

    @app.step
    async def process(x: int, services=None):
        return x * 2

    @app.workflow
    async def my_workflow(value: int, services=None):
        result = await process(value, services=services)
        return result

    await app.publish()

    # Execute multiple workflows in parallel
    tasks = [
        app.my_workflow(i).wait_result()
        for i in range(10)
    ]
    results = await asyncio.gather(*tasks)
    print(f"Processed {len(results)} workflows")

if __name__ == "__main__":
    asyncio.run(main())
```

### Error Handling Pattern

```python
import asyncio
from blazing import Blazing

async def main():
    app = Blazing()

    @app.step
    async def risky_operation(x: int, services=None):
        if x < 0:
            raise ValueError("Value must be non-negative")
        return x * 2

    @app.workflow
    async def safe_workflow(value: int, services=None):
        try:
            result = await risky_operation(value, services=services)
            return {"success": True, "result": result}
        except ValueError as e:
            return {"success": False, "error": str(e)}

    await app.publish()

    # Handle both success and failure
    result = await app.safe_workflow(-5).wait_result()
    print(result)  # {'success': False, 'error': 'Value must be non-negative'}

if __name__ == "__main__":
    asyncio.run(main())
```

## Next Steps

### Core Concepts

- [Workflows Overview](/docs/workflows/overview) - Workflow architecture
- [Steps Reference](/docs/steps/overview) - Step types and patterns
- [Services](/docs/services/overview) - Service injection

### Advanced Features

- [HTTP Endpoints](/docs/endpoints/overview) - Web API patterns
- [Middleware](/docs/endpoints/middleware) - Request/response processing
- [Authentication](/docs/endpoints/authentication) - Secure endpoints

### Deployment

- [CLI Reference](/docs/cli/reference) - Command-line tools
- [Configuration](/docs/configuration/overview) - Environment setup
- [Monitoring](/docs/monitoring/overview) - Observability

---

**All tutorials use current Blazing Flow patterns** - aligned with PATTERNS.md and the latest SDK API.
