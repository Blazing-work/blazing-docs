---
title: Configuration Reference
label: Configuration Reference
description: Complete reference for Blazing Gateway configuration. Click sections to highlight corresponding YAML.
position: 0
yamlFile: gateway-config.yaml
showYamlPane: true
yamlSections:
  - id: version
    title: Header & Version
    path: version
    description: Configuration file header with version identifier.
    details:
      - "version: Schema version (currently 1)"
  - id: gateway
    title: Gateway Settings
    path: gateway
    description: Core gateway identification and high availability settings.
    details:
      - "name: Unique gateway identifier"
      - "regions: Geographic deployment regions"
      - "ha: High availability with min/max instances"
  - id: sip
    title: SIP Configuration
    path: sip
    description: Session Initiation Protocol server settings, authentication, and TLS.
    subSections:
      - id: sip-timers
        title: SIP Timers
        path: sip.timers
        description: RFC 3261 timer configuration including T1 (RTT estimate), T2 (max retransmit interval), and T4 (max message network duration).
      - id: sip-transports
        title: Transport Protocols
        path: sip.transports
        description: Supported protocols including UDP, TCP, TLS, WebSocket (ws), and Secure WebSocket (wss).
      - id: sip-authentication
        title: Authentication
        path: sip.authentication
        description: Digest authentication (RFC 2617) or certificate-based auth. Supports local user database or external auth endpoint.
      - id: sip-tls
        title: TLS/Encryption
        path: sip.tls
        description: Certificate configuration, minimum TLS version, and cipher suite selection for secure signaling.
  - id: media
    title: Media Configuration
    path: media
    description: RTP/SRTP settings, codecs, transcoding, and NAT traversal.
    subSections:
      - id: media-codecs
        title: Audio Codecs
        path: media.codecs
        description: Priority-ordered list of supported codecs including Opus (wideband), G.722, PCMU/PCMA (G.711), and G.729.
      - id: media-srtp
        title: SRTP Encryption
        path: media.srtp
        description: Secure RTP configuration with crypto suites like AES_CM_128_HMAC_SHA1_80 for encrypted media streams.
      - id: media-processing
        title: Media Processing
        path: media.processing
        description: Transcoding, call recording, conference mixing, and noise suppression capabilities.
      - id: media-nat
        title: NAT Traversal
        path: media.nat_traversal
        description: STUN/TURN server configuration and ICE support for reliable media delivery behind NAT.
  - id: routing
    title: Routing Configuration
    path: routing
    description: Call routing rules, load balancing, and failover policies.
    subSections:
      - id: routing-rules
        title: Routing Rules
        path: routing.rules
        description: Pattern-based routing with prefix matching, exclusions, and multi-provider failover with weighted distribution.
      - id: routing-load-balancing
        title: Load Balancing
        path: routing.load_balancing
        description: Health check configuration with OPTIONS pings, thresholds, and weighted round-robin distribution.
      - id: routing-failover
        title: Failover Policy
        path: routing.failover
        description: Automatic failover triggers based on error rate, latency thresholds, and consecutive failures.
  - id: security
    title: Security Configuration
    path: security
    description: DDoS protection, access control, and fraud prevention.
    subSections:
      - id: security-ddos
        title: DDoS Protection
        path: security.ddos_protection
        description: Rate limiting per IP, per user, and global limits to prevent denial of service attacks.
      - id: security-access
        title: Access Control
        path: security.access_control
        description: IP whitelist/blacklist with strict mode and auto-blacklisting for repeated offenders.
      - id: security-fraud
        title: Fraud Prevention
        path: security.fraud_prevention
        description: Cost limits, pattern detection for suspicious activity, and premium rate number blocking.
  - id: monitoring
    title: Monitoring Configuration
    path: monitoring
    description: Metrics, logging, and alerting settings.
    subSections:
      - id: monitoring-metrics
        title: Metrics & Prometheus
        path: monitoring.metrics
        description: Prometheus endpoint configuration with custom metrics for call volume, duration, and MOS quality scores.
      - id: monitoring-logging
        title: Logging
        path: monitoring.logging
        description: Multi-destination logging (stdout, file, syslog, S3) with rotation policies and JSON formatting.
      - id: monitoring-alerts
        title: Alerting Rules
        path: monitoring.alerts
        description: Condition-based alerts with severity levels and multi-channel notifications (email, Slack, PagerDuty).
  - id: integration
    title: Integration Configuration
    path: integration
    description: Blazing Core integration and webhook endpoints.
    subSections:
      - id: integration-core
        title: Core Services
        path: integration.core
        description: Connect to Blazing Core services with event subscriptions for call lifecycle events.
      - id: integration-webhooks
        title: Webhooks
        path: integration.webhooks
        description: External webhook endpoints with custom headers, event filtering, and exponential backoff retry.
---
