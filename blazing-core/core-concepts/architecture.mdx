---
title: Architecture Overview
label: Architecture
description: "Blazing Core Compose uses a thin client, smart server architecture that separates validation from orchestration for maximum reliability and consistency."
position: 0
---

Blazing Core Compose uses a **thin client, smart server** architecture that separates validation from orchestration for maximum reliability and consistency.

## Design Principles

### Server-Side Intelligence

The Core Compose compiler uses **server-side intelligence** for infrastructure decisions. This design choice provides several critical benefits:

1. **Crash Resilience**: CLI survives crashes while server continues orchestration
2. **Consistency**: Same placement algorithm for all clients (CLI, Web UI, mobile)
3. **Real-Time Data**: Server has current availability, pricing, and quota information
4. **Atomic Operations**: All resources created or none (no partial deployments)

## Architecture Layers

### Client Layer (Thin CLI)

The CLI is responsible for:

```yaml
responsibilities:
  - Fast local validation (50-200ms)
  - Schema compliance checks
  - Feature and control validation
  - Intent submission to API
  - Display results and feedback
```

**What the CLI Does NOT Do:**
- Make infrastructure placement decisions
- Track provider availability
- Handle resource provisioning
- Manage state

### Server Layer (Control Plane API)

The Control Plane API orchestrates all infrastructure:

```yaml
responsibilities:
  - Real-time provider scoring
  - Intelligent placement algorithms
  - Resource provisioning
  - State management
  - Audit logging
  - Cost estimation
```

### Provider Layer

Provider compilers translate Core Compose specifications into native formats:

- **DFC Compiler**: Generates Akash SDL + Gateway configs
- **AWS Compiler**: CloudFormation/Terraform for EKS
- **GCP Compiler**: GKE manifests + Cloud resources
- **Azure Compiler**: AKS + ARM templates

## Deployment Flow

### Phase 1: Local Validation (Client)

```bash
core validate compose.yaml
```

The CLI performs fast local checks:

1. **Schema Validation**: YAML structure and types
2. **Feature Checks**: Verify requested features are available
3. **Control Checks**: Ensure compliance requirements can be met
4. **Policy Validation**: Org-level policy conformance

**Time**: 50-200ms
**Result**: Immediate feedback on configuration issues

### Phase 2: Plan Intent (Client)

```bash
core plan compose.yaml
```

The CLI shows deployment **intent** (what will happen):

- Services to be deployed
- Resource requirements
- Provider preferences
- Compliance controls
- Estimated cost range

**Important**: Actual region selection happens server-side during `apply`.

### Phase 3: Orchestration (Server)

```bash
core apply compose.yaml
```

The server executes the deployment:

#### 3.1 Placement Algorithm

```python
def score_provider(provider, requirements):
    score = 0

    # Region availability
    if provider.has_capacity(requirements.cpu, requirements.memory):
        score += 40

    # Cost efficiency
    cost_score = calculate_cost_efficiency(provider, requirements)
    score += cost_score * requirements.strategy.cost / 100

    # Latency optimization
    if provider.region in requirements.preferred_regions:
        score += requirements.strategy.latency

    # Compliance match
    if provider.meets_compliance(requirements.controls):
        score += requirements.strategy.compliance

    # Feature availability
    if all_features_available(provider, requirements.features):
        score += 20
    else:
        score -= 30  # Penalize missing features

    return score
```

#### 3.2 Resource Provisioning

1. **Virtual Allocation**: Reserve IPs, GPUs, storage
2. **Infrastructure Creation**: Provision compute nodes
3. **Network Setup**: Configure ingress/egress
4. **Service Deployment**: Deploy containers
5. **Verification**: Health checks and readiness

All operations are **atomic**: if any step fails, entire deployment rolls back.

#### 3.3 Response

Server returns:

```json
{
  "deployment_id": "df-2025-12-03-00123",
  "regions_created": [
    {"provider": "GCP", "region": "us-central1", "nodes": 3},
    {"provider": "DFC", "region": "chicago", "nodes": 2}
  ],
  "placement_rationale": [
    "GCP selected for reliability (score: 91)",
    "DFC selected for cost efficiency (60% savings)"
  ],
  "cost_estimate": {
    "monthly": 191,
    "variance": 15
  },
  "endpoints": {
    "web": "https://web-my-cluster.blazing.app"
  }
}
```

## State Management

### State Registries

| Registry | Purpose | Backend |
|----------|---------|---------|
| IPLease | Tracks leased IPs and timers | etcd / Consul |
| CertRegistry | TLS certificates & rotation | Vault |
| FeatureRegistry | features.yaml snapshot | Git |
| ControlRegistry | controls.yaml snapshot | Git |
| PlanCache | Last effective plan | Redis |

### Audit Trail

Every deployment creates an **immutable audit record**:

```json
{
  "deployment_id": "df-2025-12-03-00123",
  "action": "apply",
  "actor": "user@example.com",
  "hash": "sha256:8f9d...",
  "timestamp": "2025-12-03T22:00:00Z",
  "features_used": {
    "GPU-SHARING": "available",
    "EGR-LOCK-COUNTRY": "available"
  },
  "controls_evaluated": {
    "PCI-DSS-4.0": "passed",
    "DATA-RESIDENCY-EU": "passed"
  },
  "providers": ["GCP", "DFC"],
  "cost_actual": 187.43
}
```

## Component Interaction

```
┌──────────────┐
│   CLI        │  Local validation (fast)
│   (Thin)     │  Submit intent to API
└──────┬───────┘
       │
       │ HTTPS (declarative request)
       │
       ▼
┌──────────────────────────┐
│  Control Plane API       │
│  (Smart Orchestrator)    │
│                          │
│  • Placement Algorithm   │
│  • Provider Scoring      │
│  • Resource Provisioning │
│  • State Management      │
│  • Audit Logging         │
└────────┬─────────────────┘
         │
         │ Provider-specific API calls
         │
    ┌────┴────┬──────┬──────┬──────┐
    ▼         ▼      ▼      ▼      ▼
┌──────┐ ┌──────┐ ┌────┐ ┌────┐ ┌──────┐
│ GCP  │ │ DFC  │ │AWS │ │Azure│ │Akash│
│ GKE  │ │ SDL  │ │EKS │ │ AKS │ │ Net │
└──────┘ └──────┘ └────┘ └────┘ └──────┘
```

## Benefits of This Architecture

### 1. Reliability
- CLI crashes don't affect deployments
- Server continues orchestration independently
- Retries and error handling server-side

### 2. Consistency
- Same placement algorithm for all clients
- No version skew between CLI and logic
- Centralized decision-making

### 3. Real-Time Optimization
- Server sees current provider pricing
- Live capacity and availability data
- Dynamic cost optimization

### 4. Security
- Credentials never leave server
- Audit trail for all operations
- Centralized access control

## Next Steps

- Learn about [Provider Configuration](/docs/overview)
- Understand [Deployment Lifecycle](/docs/deployment-lifecycle)
- Explore [Governance Layer](/docs/features-and-controls)
